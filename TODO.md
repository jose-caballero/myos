* do not hardcode "admin"
    * maybe reusing class Cloud() somehow?
* use the python library "openstack" instead of running commands in subshells
* create more classes, e.g. 
    * IP
    * LoadBalancer
    * ...
